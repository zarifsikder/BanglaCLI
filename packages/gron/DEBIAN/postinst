#!/data/data/com.termux/files/usr/bin/bash

set -e

go install -trimpath -ldflags "-w -s" github.com/tomnomnom/gron@v0.7.1

if [ ! -f "$PREFIX/bin/gron" ] && [ -f "$HOME/go/bin/gron" ]; then
    ln -sf "$HOME/go/bin/gron" "$PREFIX/bin/gron"
fi

exit 0

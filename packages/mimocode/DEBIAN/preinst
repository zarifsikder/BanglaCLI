#!/data/data/com.termux/files/usr/bin/bash
ARCH=$(uname -m)
case "$ARCH" in
    aarch64|x86_64)
        exit 0
        ;;
    *)
        echo "Error: mimocode only supports aarch64 or x86_64 architectures." >&2
        echo "Your architecture: $ARCH" >&2
        exit 1
        ;;
esac
